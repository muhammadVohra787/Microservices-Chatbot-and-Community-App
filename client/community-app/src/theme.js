import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    mode: 'light',  // Ensure this is set to 'dark'
    // ... other palette settings
  },
  // ... other theme settings
});

export default theme;
